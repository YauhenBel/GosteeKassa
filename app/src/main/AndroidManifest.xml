<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.genia.gosteekassa">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".Controllers.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Controllers.EditKassa" />
        <activity android:name=".Controllers.Statistics" />
        <activity
            android:name=".Controllers.Registration"
            android:windowSoftInputMode="stateAlwaysHidden|adjustResize"
            tools:ignore="InnerclassSeparator" />
        <activity
            android:name=".Controllers.RecoveryPassword"
            android:windowSoftInputMode="stateAlwaysHidden|adjustResize"
            tools:ignore="InnerclassSeparator" />
        <activity
            android:name=".Controllers.NewPassword"
            android:windowSoftInputMode="stateAlwaysHidden|adjustResize"
            tools:ignore="InnerclassSeparator" />
        <activity android:name=".Controllers.QrScanner"></activity>
    </application>

</manifest>